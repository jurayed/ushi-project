// Простейший Express сервер
const express = require('express');
const app = express();
const port = 3000;

// Самый простой маршрут - главная страница
app.get('/', (req, res) => {
  res.send('Привет! Это работает! Уши на связи! 👂');
});

// Запускаем сервер
app.listen(port, () => {
  console.log('Сервер запущен на http://localhost:${port}'); 
});